#includestdio.h
int main()
{
    int s;
    long a, b, c, n;
    int t=0, i;
    scanf(%li %li %li, &a, &b, &c);
    scanf(%li, &n);
    s = (int) malloc(100000  sizeof(int));
    if(ab && ac)
    {
        for(i=0;in;i++)
        {
            scanf(%d, &s[i]);
        }
        for(i=0;in;i++)
        {
            if(s[i]b && s[i]c)
            {
                t++;
            }
        }
        printf(%dn, t);
    }
    else
    {
        t = 0;
        printf(%dn, t);
    }
    return 0;
}